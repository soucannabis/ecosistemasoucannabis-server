const express = require('express');
const router = express.Router();

// ✅ Este arquivo foi substituído pelo novo sistema de autenticação baseado em cookies
// As rotas de autenticação agora estão em /routes/auth.js
// As rotas protegidas agora estão em /routes/protected.js

module.exports = router;
